# FloMind Budget Dashboard — Dépendances directes
# Python 3.11+
# Généré sur : 2025-04-23
# Mise à jour : pip install -r requirements.txt
#
# Convention : versions épinglées (==) pour reproductibilité garantie.
# Ne pas utiliser pip freeze (inclut les dépendances transitives inutilement).
# Pour mettre à jour une dépendance : tester en local avant de changer la version.

# ── Interface utilisateur ──────────────────────────────────────────────────────
streamlit==1.56.0            # Dashboard web interactif

# ── Visualisation ─────────────────────────────────────────────────────────────
plotly==6.7.0                # Graphiques interactifs (waterfall, heatmap, forecast)

# ── Données ───────────────────────────────────────────────────────────────────
pandas==3.0.2                # DataFrames, agrégations, pivot tables
numpy==2.4.4                 # Calculs SIG, bootstrap P10/P90, régressions WLS

# ── Source de données ─────────────────────────────────────────────────────────
openpyxl==3.1.5              # Lecture/écriture Excel (.xlsx) — requis par pandas

# ── Utilitaires ───────────────────────────────────────────────────────────────
# (aucune dépendance utilitaire directe actuellement)

# ── Développement uniquement (ne pas inclure en production) ───────────────────
# pytest==8.3.0              # Tests unitaires — décommenter pour dev
# black==24.10.0             # Formatage code — décommenter pour dev
